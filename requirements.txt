# Zero-to-Synced — backend dependencies (UTF-8, cross-platform).
# Covers the ADK agent + Fivetran MCP server + FastAPI API layer.

# --- Agent / Gemini / MCP ---
google-adk>=2.1.0
google-genai>=1.75.0
python-dotenv>=1.0.0
mcp>=1.25.0
httpx>=0.28.0

# --- API layer ---
fastapi>=0.136.0
uvicorn>=0.48.0
sse-starlette>=3.4.0
python-multipart>=0.0.20

# --- Database (Neon Postgres via async SQLAlchemy) ---
SQLAlchemy[asyncio]>=2.0.0
asyncpg>=0.31.0

# --- File ingestion ---
pandas>=2.2.0
openpyxl>=3.1.0     # required by pandas for .xlsx
boto3>=1.40.0       # optional: only used when S3_BUCKET is set

# --- Auth ---
cryptography>=42.0.0
bcrypt>=4.1.0
PyJWT>=2.9.0
email-validator>=2.1.0
