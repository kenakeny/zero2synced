# Optional. Leave blank to use the Vite dev proxy (recommended for local dev).
# Set to your deployed API origin in production, e.g. https://api.your-app.com
VITE_API_BASE=

# Where `npm run dev` proxies /api to (defaults to http://localhost:8000)
VITE_API_TARGET=http://localhost:8000
